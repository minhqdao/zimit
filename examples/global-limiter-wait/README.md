# global-limiter-wait Example

This is a simple demonstration of the `GlobalLimiter` using `wait()` where requests are automatically delayed until they can proceed without exceeding the global rate limit.

## Usage

Run `zig build run`.

## Requirements

Zig 0.16.0 or later.
